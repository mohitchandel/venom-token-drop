import * as nt from 'nekoton-wasm';
export declare abstract class ConnectionFactory {
    abstract create(clock: nt.ClockWithOffset): nt.ProxyConnection;
}
/**
 * @category Client
 */
export type ProxyParams = {
    connectionFactory: ConnectionFactory;
};
